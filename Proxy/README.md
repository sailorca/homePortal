<!-- markdownlint-disable MD032 -->
# Public to Private Proxy Settings

This directory contains:
- (obsolete) aws_demo_server_guide.md - description of proxy framework that uses tokens and subdomains to provide access to demos on private server
- (obsolete) nginx proxy on public server (aws ec2) with Let's Encypt certs and demo token support
- frps and frpc (ssh) config on public server
- frpc config on bigdog (home server)
- (obsolete) demo system with tokens (obsolete)
